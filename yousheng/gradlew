#!/bin/bash

# Gradle wrapper script for Linux/Mac
if [ ! -f "./gradle/wrapper/gradle-wrapper.jar" ]; then
    echo "Gradle wrapper not found. Please download Android Studio and open this project first."
    exit 1
fi

./gradle/wrapper/gradle-wrapper.jar "$@"
